# -*- coding: utf-8 -*-
"""
Original standalone GUI script placeholder.

The full original `auto.py` is preserved in the repository history at:

    legacy-v0.1/legacy/auto.py

This file is intentionally kept as a small pointer in `main` so the
`original_files/` folder documents the alternative legacy entry point without
making the packaged skill import or execute the old GUI code.

To restore the exact legacy file locally, run from the repository root:

    git show legacy-v0.1:legacy/auto.py > original_files/auto.py

Then run:

    python original_files/auto.py

See `original_files/README.md` for environment requirements and safety notes.
"""

from __future__ import annotations

print(
    "The full original auto.py is stored at legacy-v0.1:legacy/auto.py.\n"
    "Restore it with:\n"
    "  git show legacy-v0.1:legacy/auto.py > original_files/auto.py\n"
    "Then run:\n"
    "  python original_files/auto.py\n"
)
